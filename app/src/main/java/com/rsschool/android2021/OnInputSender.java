package com.rsschool.android2021;

/**
 * Interface definition for a callback to be invoked when input data is ready to send.
 */
public interface OnInputSender {
    void sendInput(int firstInput, int secondInput);
}
